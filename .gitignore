# Python-generated files
__pycache__/
*.py[oc]
build/
wheels/
*.egg-info

# Virtual environments
.venv

# Node / frontend
node_modules/
web/dist/
web/.vite/
*.local

# Crawl artifacts: large binaries stay out of the repo (deterministically
# re-fetchable via source/fetch_all.py). The parsed HTML/JSON is committed.
source/raw/images/
source/raw/files/
source/tiles/

# OS
.DS_Store
Thumbs.db
